syntax = "proto3";
package node;

import "github.com/aperturerobotics/hydra/cid/cid.proto";

// NodeState contains the state of a Node.
message NodeState {
  // LastMsgRef is the reference to the last node message.
  cid.BlockRef last_msg_ref = 1;
}