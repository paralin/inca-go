syntax = "proto3";
package peer;

import "github.com/aperturerobotics/inca/inca.proto";

// PeerState is information about a peer stored in the database.
message PeerState {
  // LastObservedMessage is the last observed message from the peer.
  inca.NodeMessage last_observed_message = 1;
}